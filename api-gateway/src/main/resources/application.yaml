spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
          routes:
            - id: produits-service
              uri: lb://produits-service
              predicates:
                - Path=/api/produits/**
            - id: categories-service
              uri: lb://produits-service
              predicates:
                - Path=/api/categories/**
            - id: avis-service
              uri: lb://avis-service
              predicates:
                - Path=/api/avis/**

server:
  port: 8090

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/